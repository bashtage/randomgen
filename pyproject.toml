[build-system]
requires = [
    "packaging>=21.0",
    "setuptools",
    "wheel",
    "Cython>=3.0.10,<4",
    "numpy>=2.0.0rc1,<3",
    "setuptools_scm[toml]>=8.0.0,<9.0.0",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "randomgen/_version.py"
